# Optional plugins

English | [中文](README.zh.md)

Private profile plugins are built and checked independently of the published package tier. pnpm manages their dependencies as workspaces; they are not mounted by default.

- [Kitty](kitty/README.md): control existing local Kitty terminals from the web sidebar. Run `pnpm run check:kitty` from the repository root.
